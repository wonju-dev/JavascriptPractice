import React from "react";
import { useRecoilValue } from "recoil";
import { withPostList } from "../recoil/post";

export const Post = () => {
  // const postList = useRecoilValue(withPostList("dummy.json"));
  // console.log(postList);
  // const li = postList.map((value, index) => <li key={index}>{value}</li>);
  return <ul>hihi</ul>;
};
